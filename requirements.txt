# Core dependencies (install first)
torch>=2.0.0
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# PyTorch Geometric (install after torch)
torch-geometric>=2.3.0
# Note: torch-scatter, torch-sparse, torch-cluster require compilation
# Install them separately if needed: pip install torch-scatter torch-sparse torch-cluster

# Data processing
scikit-learn>=1.0.0
networkx>=2.8.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0
tensorboard>=2.10.0

# Utils
tqdm>=4.64.0
pyyaml>=6.0
jupyter>=1.0.0
